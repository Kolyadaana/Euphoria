#include <iostream>
#include <math.h>
#include <string>

using namespace std;

int main()
{
	setlocale(LC_ALL, "Russian");
	int N, i;
	double S, a;
	cout << "Введите число" << endl;
	cin >> N;
	i = 1;
	S= a = 1.1;
		while (i < N)
		{
			S *= (a + 0.1);
			i += 1;
			a += 0.1;
		}
		cout << S;
}
